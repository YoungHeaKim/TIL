var num = 10; // num이라는 변수를 선언해주고 0이라는 숫자를 할당해준다.
while (num >= 0){ // num이라는 숫자가 10보다 작을 때까지 반복해준다.
  if (num%2 == 1){ // num이라는 숫자를 2로 나누면 0이되는 수는 짝수를 검증하는 식이다.  
    console.log(num); // num이라는 수를 출력한다.
  }
  num--; // num이라는 수를 증가시킨다.  
}
